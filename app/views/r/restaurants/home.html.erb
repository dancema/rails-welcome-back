<h2>Dashboard</h2>



  <% @restaurants.each do |restaurant| %>
    <h2><%= restaurant.name %></h2>
    <%= line_chart current_user.restaurants.first.stars.group_by_day(:created_at).count %>
  <% end %>

<li>
  <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
</li>
